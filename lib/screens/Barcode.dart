import 'package:flutter/material.dart';

class BarcodeScreen extends StatelessWidget {
  static const String id = 'BarcodeScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Barecode Screen'),
      ),
    );
  }
}
